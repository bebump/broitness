It uses reasonably little resources. It needs about 4 MB of RAM and
practically zero percent of your CPU.

It is conservative with sending brightness change commands, as some monitors
write this value into a finite lifetime EEPROM.